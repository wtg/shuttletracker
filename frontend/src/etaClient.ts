import UserLocationService from '@/structures/userlocation.service';
import store from '@/store';

class ETAClient {
    // private callbacks = Array<(message: {}) => void>();

    // public start() {
    //     // watch estimated times of arrival to determine if we should show a message
    //     store.watch((state) => state.etas, (newValue, oldValue) => {

    //     });
    // }
}


// const store: StoreOptions<StoreState> = {
//     state: {
//         etas: []string,
//     }
// };
